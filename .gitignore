result
my-test-suite.log
node_modules/
.decapod/data
.decapod/data/*
.decapod/.stfolder
.decapod/workspaces
.decapod/generated/
.decapod/generated/*
!.decapod/data/
!.decapod/generated/Dockerfile
!.decapod/generated/context/
!.decapod/generated/context/*.json
!.decapod/generated/policy/
!.decapod/generated/policy/context_capsule_policy.json
!.decapod/generated/artifacts/
!.decapod/generated/artifacts/provenance/
!.decapod/generated/artifacts/provenance/*.json
!.decapod/generated/artifacts/provenance/kcr_trend.jsonl
!.decapod/generated/specs/
!.decapod/generated/specs/*.md
!.decapod/generated/specs/.manifest.json
# Managed runtime outputs are current-run state, not the governance record:
# version_counter.json and migrations/applied.json carry per-machine timestamps.
# Mirrors upstream's own rules, which keep only the container baseline and specs.
.decapod/managed/*
!.decapod/managed/
!.decapod/managed/Dockerfile.decapod
!.decapod/managed/specs/
!.decapod/managed/specs/*.md
!.decapod/managed/specs/.manifest.json

# RPI workflow
.opencode/

# RPI workflow
.claude/

# RPI workflow
.rpi/*
!.rpi/specs/
/.agent-shell/

# CodeGraph index (generated)
.codegraph/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# MCP Agent Mail live SQLite store (default DATABASE_URL=./storage.sqlite3)
storage.sqlite3
storage.sqlite3-*
archived_mailbox_states/

# Oracle Cloud Terraform local state and secrets
terraform/oracle/.terraform/
terraform/oracle/terraform.tfstate
terraform/oracle/terraform.tfstate.backup
terraform/oracle/terraform.tfvars
terraform/oracle/*.auto.tfvars

# Nix build output for Oracle OCI image
result/

# Chorus runtime state/lock
.chorus/

# Local scratch space for in-flight investigations
.scratch/

# Cave agent worktrees and session scratch.
.cave/
.decapod/governance/workunits/

# Decapod entrypoint regeneration backups (`decapod init --force`).
*.md.bak
.decapod_archive/

# Graphiq knowledge-graph index (1.3M SQLite, regenerated on demand).
.graphiq/
